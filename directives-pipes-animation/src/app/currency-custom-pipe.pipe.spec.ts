import { CurrencyCustomPipePipe } from './currency-custom-pipe.pipe';

describe('CurrencyCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
